[Unit]
Description=Ansible Agent
After=network.target

[Service]
Type=oneshot
WorkingDirectory=/etc/ansible-agent
ExecStart=/usr/local/ansible-agent/bin/ansible-pull --url {{ git_url }} --checkout {{ git_ref }} --inventory /etc/ansible-agent/inventory playbook.yaml

[Install]
WantedBy=multi-user.target
